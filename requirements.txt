# DataFlow — Streamlit Cloud compatible requirements
# Streamlit Cloud uses Python 3.14 and cannot be changed.
# All version floors are set to get Python-3.14 compatible pre-built wheels.
#
# Key constraint chain that was breaking:
#   streamlit==1.35.0 → pillow<11 → no Python 3.14 wheel → build fails
#   streamlit>=1.40.0 → pillow>=11 → has Python 3.14 wheel → works
#
# Render uses Python 3.11.9 (pinned via .python-version) so exact versions
# don't matter there — it always finds 3.11 wheels regardless.

pandas>=2.2.0
numpy>=1.26.0
requests>=2.31.0
pyyaml>=6.0.1
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
streamlit>=1.40.0
apscheduler>=3.10.4
sqlalchemy>=2.0.30
python-dotenv>=1.0.1
httpx>=0.27.0
pytest>=8.2.0
pytest-asyncio>=0.23.7
plotly>=5.22.0
altair>=5.3.0
rich>=13.0.0
tabulate>=0.9.0
